<?xml version="1.0" encoding="utf-8"?>
<!-- Highlighted list row. Used for the AutoConnect config generator bot entry. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android" android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFFFF" />
            <corners android:radius="18dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/brand_soft" />
            <stroke android:width="1dp" android:color="@color/brand" />
            <corners android:radius="18dp" />
        </shape>
    </item>
</ripple>
